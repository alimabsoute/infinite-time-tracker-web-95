import Contact from '@/pages/Contact';

/**
 * ContactPage — thin shell
 * Delegates to the existing Contact component (contact form with
 * email support info and FAQ links).
 */
export default function ContactPage() {
  return <Contact />;
}
