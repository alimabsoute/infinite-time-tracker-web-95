import PrivacyPolicy from '@/pages/PrivacyPolicy';

/**
 * PrivacyPage — thin shell
 * Delegates to the existing PrivacyPolicy component (static legal page
 * with data collection, usage, sharing, security, and rights sections).
 */
export default function PrivacyPage() {
  return <PrivacyPolicy />;
}
