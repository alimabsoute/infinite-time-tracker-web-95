import Signup from '@/features/auth/components/Signup';

/**
 * SignupPage — thin shell
 * Delegates to the auth feature's Signup component with email/password
 * registration form and login link.
 */
export default function SignupPage() {
  return <Signup />;
}
