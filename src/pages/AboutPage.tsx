import About from '@/pages/About';

/**
 * AboutPage — thin shell
 * Delegates to the existing About component (static marketing page
 * with mission, features, story, and CTA sections).
 */
export default function AboutPage() {
  return <About />;
}
