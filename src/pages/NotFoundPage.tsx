import NotFound from '@/pages/NotFound';

/**
 * NotFoundPage — thin shell
 * Delegates to the existing NotFound component (404 error page
 * with return-to-home link).
 */
export default function NotFoundPage() {
  return <NotFound />;
}
