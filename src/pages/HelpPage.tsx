import Help from '@/pages/Help';

/**
 * HelpPage — thin shell
 * Delegates to the existing Help component (FAQ sections with search,
 * collapsible categories, and roadmap features).
 */
export default function HelpPage() {
  return <Help />;
}
