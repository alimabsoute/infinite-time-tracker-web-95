import Settings from '@/pages/Settings';

/**
 * SettingsPage — thin shell
 * Delegates to the existing Settings component which renders Header,
 * Account/Subscription/Activity/Security tabs with profile management.
 */
export default function SettingsPage() {
  return <Settings />;
}
